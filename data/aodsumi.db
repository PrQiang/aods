/*
本数据文件采用json格式:
结构形如:
{
    "项目名称":{
		"模块名称":{
		"WorkDir":"该模块工作目录，支持相对路径和绝对路径",
		"Command":"客户端更新重启命令, 如aods服务的重启命令 service aods restart"
		}
	}
}

说明：
1. 第一层键值为管理项目名称，第二层键值为模块名称，第三层为模块信息，其中:
	WorkDir: 为工作目录，支持相对路径和绝对路径两种形式，相对路径请在路径前加., 如./pyfbc
	Command: 为项目更新后重启命令, 如aods服务的重启命令 service aods restart
2. aods支持多个项目更新维护，同一个项目也可以有多个模块

注1: aods在更新文件时采用先移动需要，在重启前可实现执行文件更新
注2: 不同项目模块工作目录建议不同，避免产生冲突
注3: aods采用system启动重启命令，并根据执行退出码是否为0来判断是否执行成功， 0为执行成功，否则为执行失败
注4: 重启命令不能堵塞执行，否则会影响aods模块后续部署更新
注5: 部署使用时，请删除注释内容
*/

{
    "aods":{
	"aods-x64-linux":{
		"WorkDir":"./",
		"Command":"service aods restart"
		}
	}
}